#version 330 core
out vec4 FragColor;

uniform sampler2D tex0;

in vec2 fragPos;
in vec4 clip;
in vec2 texPos;

uniform bool    drawBackground;
uniform vec3    color;

void main()
{
    FragColor = texture(tex0, texPos);
}
