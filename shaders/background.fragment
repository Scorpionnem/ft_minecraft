#version 330 core

out vec4 FragColor;

uniform sampler2D tex;
uniform float scale;

void main()
{
	int	zoomFactor = 32;
    vec4 sampled = vec4(texture(tex, gl_FragCoord.xy / (scale * zoomFactor)).rgb / 2, 1.0);
    FragColor = sampled;
}
